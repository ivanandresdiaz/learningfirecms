/* eslint-disable @typescript-eslint/no-explicit-any */
import React from "react";

const Preview = (props: any) => {
  console.log("props", props);
  return <div>Preview DISPLAY DATA</div>;
};

export default Preview;
